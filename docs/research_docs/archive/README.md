# Archive

Research for concepts that were considered and dropped. Kept because
*"what else did you consider?"* is a near-certain question in the walkthrough,
and "I dropped it for this reason" is a better answer than reconstructing it live.

- `physio-workflow-research.md` — a clinical reasoning assistant for
  physiotherapists. Dropped: the demo needed ~90 seconds of briefing before it
  landed, and a reviewer cannot judge whether an MSK differential is any good.
  Useful finding regardless: physio referral labels are usually empty (only 32%
  anatomically specific), so the reasoning happens whoever referred the patient.
