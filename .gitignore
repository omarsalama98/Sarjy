# Secrets
.env
.env.*
!.env.example

# Specs are working documents, not deliverables
*.spec.md

# Dependencies / build
node_modules/
dist/
build/
.next/
__pycache__/
*.pyc
.venv/

# Copied from node_modules by sync-vad-assets.mjs on every dev/build -- not
# a source file, and the models/WASM are binary (frontend/scripts/sync-vad-assets.mjs).
frontend/public/vad/

# Claude Code local state
.claude/settings.local.json
.claude/projects/

# OS
.DS_Store

# Strategic working notes — not for the submitted repo.
# Research documents ship (they show the rigor); decision sheets stay local.
docs/decisions/

# Python
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local data caches (vendored reference data IS committed — see data/README)
data/cache/

# Block 0 spike scratch — throwaway, never committed
scratch/
